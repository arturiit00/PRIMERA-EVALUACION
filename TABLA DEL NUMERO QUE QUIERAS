# Escribimos la tabla que elija el
# usuario
def tabla_1():
    ntabla=input("Dime del numero de tabla: ")
    for cont in range(1,11):
        print(str(ntabla)+"*"+str(cont)+"= "+str(int(ntabla)*cont))

tabla_1()
